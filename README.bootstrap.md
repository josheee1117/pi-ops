# Pi-Ops bootstrap note

This repository is being initialized as a TypeScript monorepo for the Pi-Ops central agent, per-node observer, and shared protocol. The canonical project README is finalized during bootstrap before merge to `main`.

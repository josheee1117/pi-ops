{
  "name": "@pi-ops/agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "echo 'lint not configured yet'"
  },
  "dependencies": {
    "@pi-ops/protocol": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}

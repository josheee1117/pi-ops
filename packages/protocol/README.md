# @pi-ops/protocol

Canonical cross-process contract for OpsEvent, EventBatch, Evidence and API schemas.

Do not duplicate protocol types independently in `apps/agent` and `apps/node-agent`.

The actual schemas are intentionally left for Milestone 1 of PLAN-0001.

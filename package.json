{
  "name": "pi-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Event-driven AI operations platform with central reasoning and per-node observers.",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  }
}
